import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF007BFF); // Example: Blue
  static const Color secondaryColor = Color(0xFF6C757D); // Example: Grey
  static const Color accentColor = Color(0xFF28A745);   // Example: Green

  // Ensure these are present as used in blog_screen.dart
  static const Color darkGrey = Color(0xFF424242);
  static const Color lightGrey = Color(0xFFF5F5F5);
  static const Color primary = primaryColor; // Alias primary to primaryColor for consistency
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}